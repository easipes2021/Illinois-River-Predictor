venv/
.venv/

# Ignore runtime data and trained models, but keep forecasts.json tracked for the dashboard.
*.csv
*.pkl
!model_*.pkl

# Ignore Python system files
__pycache__/
*.py[cod]
.DS_Store

# The following shell commands are examples of how to remove large generated assets from tracking.
# git rm --cached master_training_data.csv
# git rm --cached model_*.pkl
# git rm --cached forecasts.json
